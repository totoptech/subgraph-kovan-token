{
  "name": "kovan-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ kovan-token",
    "create-local": "graph create --node http://localhost:8020/ kovan-token",
    "remove-local": "graph remove --node http://localhost:8020/ kovan-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kovan-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
